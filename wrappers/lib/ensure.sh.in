export EXPIDUS_SDK_DATADIR="@SDK_DATADIR@"
export EXPIDUS_SDK_LIBDIR="@SDK_LIBDIR@"
export EXPIDUS_SDK_BINDIR="@SDK_BINDIR@"
export EXPIDUS_SDK_VERSION="@VERSION@"
export EXPIDUS_SDK_HOST="@HOST_SYSTEM@"
export EXPIDUS_SDK_TARGET="@TARGET_SYSTEM@"

export PATH=:$PATH
export PATH="${EXPIDUS_SDK_BINDIR}${PATH//:$EXPIDUS_SDK_BINDIR/}"

export GI_TYPELIB_PATH=:$GI_TYPELIB_PATH
export GI_TYPELIB_PATH="${EXPIDUS_SDK_LIBDIR}/girepository-1.0${GI_TYPELIB_PATH//:${EXPIDUS_SDK_LIBDIR}\/girepository-1.0/}"

export PKG_CONFIG_PATH=:$PKG_CONFIG_PATH
export PKG_CONFIG_PATH="${EXPIDUS_SDK_LIBDIR}/pkgconfig${PKG_CONFIG_PATH//:${EXPIDUS_SDK_LIBDIR}\/pkgconfig/}"

if [[ ! -z "@NIX_CHANNELDIR@" ]]; then
  # FIXME: check if path is not already added
  export NIX_PATH="nixpkgs=$EXPIDUS_SDK_LIBDIR/nix/channel:$NIX_PATH"
fi

#if [[ ! -z "@NIX_PROFILEDIR@" ]]; then
# TODO: load our custom nix profile
#fi
